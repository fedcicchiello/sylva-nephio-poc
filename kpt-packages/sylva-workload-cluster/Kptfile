apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: sylva-workload-cluster # kpt-set: sylva-${${custerName}Type}-${custerName}
  annotations:
    config.kubernetes.io/local-config: "true"
info:
  description: Manage the lifecycle of a Sylva workload cluster # kpt-set: Manage the lifecycle of a Sylva ${${custerName}Type} ${custerName}
  # pipeline:
  #   mutators:
  #   - image: gcr.io/kpt-fn/starlark:v0.5.0
  #     configPath: set-values.yaml
  # TODOs: mutators: setters + (starlark) and validators
